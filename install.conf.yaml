- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/Library/Preferences/com.knollsoft.Rectangle.plist:
        path: rectangle.plist


# `force: true` when creating a new file