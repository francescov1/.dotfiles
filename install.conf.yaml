- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/.config/karabiner/karabiner.json:
        path: karabiner.json
        force: true
    ~/.aliases: aliases

